import { SearchSujetPipe } from './search-sujet.pipe';

describe('SearchSujetPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchSujetPipe();
    expect(pipe).toBeTruthy();
  });
});
